# WirelessBOSS's primary interface is now the web UI (wirelessboss/server) -
# a small local web server + browser, same launch pattern as Kismet itself.
fastapi>=0.110
uvicorn>=0.27
pydantic>=2.5
requests>=2.31
PyYAML>=6.0

# Optional: only needed for the legacy PyQt6 desktop GUI (wirelessboss/app.py,
# `python -m wirelessboss.app`). Not required for the web UI - skip these
# unless you specifically want the old desktop app too. On Kali, prefer apt
# for the Qt/WebEngine pieces over pip (pip wheels for PyQt6-WebEngine are
# large and sometimes lag Qt versions shipped by apt):
#   sudo apt install python3-pyqt6 python3-pyqt6.qtwebengine
# PyQt6>=6.6
# PyQt6-WebEngine>=6.6
